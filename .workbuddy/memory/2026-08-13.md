# 2026-08-13 — i18n refactor + mobile optimization

## What was done

1. **Created `theme/i18n.ts`** — Centralized all page content strings (section titles, categories, steps, FAQs, CTA, floating cards, marketplace hero/buttons/empty/error states) for all 7 languages. Removed inline i18n data from Vue components.

2. **Created `theme/SiteFooter.vue`** — Shared footer component that reads from VitePress `themeConfig.footer` (already configured in `config.ts` via `tc()`). Replaced duplicated footer markup in both AdvancedHome and MarketplaceView.

3. **Refactored `AdvancedHome.vue`** — Now imports from i18n.ts, uses `useData().lang` to select locale, uses `localeLink()` helper for locale-aware links, uses SiteFooter. Removed all hardcoded English strings and inline footer. Floating cards rendered from i18n data with dynamic SVG icons.

4. **Refactored `MarketplaceView.vue`** — Removed inline HERO object (7 languages), now imports from i18n.ts. All button texts, empty/error states localized. Uses SiteFooter.

5. **Optimized `SiteNav.vue` for mobile** — Below 768px: nav menu + language switcher hidden (only logo + theme toggle). Below 480px: brand text hidden, reduced nav height/padding.

6. **Updated `theme/index.ts`** — `isHomePage` regex now matches locale home paths (`/zh-CN/`, `/ja/`, etc.) so AdvancedHome layout is used for all locales.

7. **Created/translated all 6 locale index.md files**:
   - Created: zh-CN, zh-TW, ja (previously missing)
   - Translated: de, es, fr (previously English copies)
   - All have translated hero text, tagline, actions, and 6 features

8. **Translated marketplace index.md** frontmatter for zh-CN, zh-TW, ja (de/es/fr already done).

## Build result
- `vitepress build` passes with no errors
- All routes return 200 (/, /zh-CN/, /ja/, /marketplace/, /zh-CN/marketplace/)
